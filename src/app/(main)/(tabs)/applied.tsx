import { View, Text } from 'react-native'
import React from 'react'
import { AppText } from '@components/ui'

const applied = () => {
  return (
    <View>
      <AppText>applied</AppText>
    </View>
  )
}

export default applied